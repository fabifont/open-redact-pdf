pub mod targets;

pub use targets::{
    FillColor, NormalizedPageTarget, NormalizedRedactionPlan, QuadTarget, QuadTargetGroup,
    RectTarget, RedactionMode, RedactionPlan, RedactionTarget, normalize_plan,
};
